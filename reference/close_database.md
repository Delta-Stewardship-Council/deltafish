# Close connection to database

Close connection to SQLite database. Recommended at the end of every
session.

## Usage

``` r
close_database(con = NULL)
```

## Arguments

- con:

  A DBI connection object from open_database()
